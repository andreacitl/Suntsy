# Proyecto FastAPI + Appwrite

## Requisitos previos
    - Python 3.10+
    - Appwrite instalado
    - Uvicorn
    - Postman (opcional, para pruebas)

## Instalación
    sudo pacman -S python-virtualenv # instalar virtual env
    python3 -m venv venv # crear entorno virtual
    source mi_entorno/bin/activate.fish # activar entorno

## Generar archivo de dependencias
    pip freeze > requeriments.txt

## Instalar dependencias
    pip install -r requirements.txt

## Run uvicorn
    uvircorn main:app

## Ruta postman
    http://127.0.0.1:8000


    
